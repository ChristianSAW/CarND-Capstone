#!/bin/bash
# Script to automatically set up the git configuration within the workspace 
# so you dont have to redo this every time
git config --global user.name "Christian Welling"
git config --global user.email "csw73@cornell.edu"
echo Set up Git Configuration!